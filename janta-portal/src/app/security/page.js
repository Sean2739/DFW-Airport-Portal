import Security from '@/components/security';

export const metadata = {
  title: "Janta Power ⋅ Settings",
};

export default function SecurityPage() {
  return (
    <div>
      <Security />
    </div>
  );
}
